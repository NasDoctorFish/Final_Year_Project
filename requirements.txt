# Core
androguard>=4.1.0        # APK decompilation + manifest parsing
numpy>=1.26              # statistics (robust-z outlier detection)
pyyaml>=6.0              # config

# AI explanation is not a client-side dependency: it runs entirely on the backend
# (backend/ has its own Node-side Gemini SDK). Nothing to install here for it.

# Desktop GUI (optional; core + CLI run without it)
PySide6>=6.6             # native desktop app: python -m bioaudit gui

# Dashboard + reporting
streamlit>=1.40          # dashboard UI
jinja2>=3.1              # HTML report templating
weasyprint>=63           # HTML -> PDF export

# Build-only: package a standalone Windows exe (see docs/BUILD-EXE.md)
# pyinstaller>=6.3

# Device control is via the `adb` CLI on PATH (no Python dependency required).
# uiautomator2 is optional and only needed for UI-driven scenario navigation:
# uiautomator2>=3.2
